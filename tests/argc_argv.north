#include <stdlib>

#define write 1
#define stdout 1

argv 
argc
while dup 0 > do
  over load64 dup strlen swap 
  stdout write syscall3 drop
  "\n" stdout write syscall3 drop
  1 - swap 
  8 + swap
done
