!=  ==  >  >=  <  <= min  max and 
&& or || not ! 
if else endif while do done 
print syscall0 syscall1 syscall2 syscall3 syscall4 syscall5 syscall6 exit 
dup 2dup drop 2drop over 2over swap
2swap rot dupnz
+ - / * %
& | ~ ^ << >>
mem load8 store8 load16 store16 load32 
store32 load64 store64
99 "String!\n"
'a' 'b' 'c' 
#include "comments.north"
#include <std.north>
#define
def ( -- ) { }
invalid
