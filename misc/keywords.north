print + - / * % 
mem store8 store16 store32 store64 load8 load16 load32 load64
exit 
dup 2dup drop 2drop over 2over swap 2swap rot dupnz
min max == != > >= < <=  
and &&   
or || 
not !
& | ~ ^ << >>
while do break continue done if else endif syscall0 syscall1 syscall2 syscall3 syscall4 syscall5 syscall6 def return
#include #define


99 "String!\n"
'a' 'b' 'c' 
#include "comments.north"
#include <string>
#define
def ( -- ) { }
invalid
